{
  "name": "dev-swarm-js-scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Shared JS/TS scripts for dev-swarm with common node_modules",
  "scripts": {
    "webui:dev": "pnpm --filter webui dev",
    "webui:build": "pnpm --filter webui build",
    "webui:start": "pnpm --filter webui start",
    "webui:lint": "pnpm --filter webui lint"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
